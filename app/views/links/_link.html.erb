
    <tr class="<%= cycle("table-active", "table-secondary") -%>">
      <td><%= link.title.nil? ? "Loading..." : link.title  %></td>
      <td><%= link.url %></td>
      <td><%= link_to link.shortened_url , link.shortened_url %></td>
       <td><%= link.clicked %></td>
    </tr>
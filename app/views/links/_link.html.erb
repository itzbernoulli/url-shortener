
    <tr class="<%= cycle("table-active", "table-secondary") -%>">
      <td><%= link.title %></td>
      <td><%= link.url %></td>
      <td><%= link_to "http://localhost:3000/" + link.slug , "http://localhost:3000/" +link.slug %></td>
       <td><%= link.clicked %></td>
    </tr>